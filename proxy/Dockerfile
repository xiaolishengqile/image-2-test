FROM node:22-alpine

WORKDIR /app

COPY package.json server.mjs ./

ENV PORT=3000
ENV TARGET_URL=https://ai.t8star.org

EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=5s --start-period=5s --retries=3 \
  CMD wget -qO- http://127.0.0.1:3000/health || exit 1

CMD ["node", "server.mjs"]
